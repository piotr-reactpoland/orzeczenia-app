import React from 'react';
import './nav-items.scss';

import {INav-itemsViewProps} from './Nav-itemsContainer';

const Nav-itemsView = (props: INav-itemsViewProps) => {
   const { } = props

   return (
   <div className='nav-items'>

   </div>
   )
} 

Nav-itemsView.defaultProps = {

} 

export default Nav-itemsView;
